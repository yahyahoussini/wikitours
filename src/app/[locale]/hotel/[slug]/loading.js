import BrandLoader from '@/components/site/BrandLoader';

/** Partner-hotel pages belong to the Omra offering — Bab Makka transition. */
export default function Loading() {
  return <BrandLoader variant="babmakka" />;
}
