import BrandLoader from '@/components/site/BrandLoader';

/** Omra month/occasion/city hubs are Bab Makka surfaces — branded transition. */
export default function Loading() {
  return <BrandLoader variant="babmakka" />;
}
