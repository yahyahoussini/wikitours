# Generator run — 2026-09-15

One batch, run after the September audit (`audit-2026-09.md`). Slots #5–#8 were
drafted, reviewed and gated; slots #9–#13 were checked against
`data/allowed-facts.json` before any drafting. The run stopped once it was clear
the next slots could not be written without inventing facts, not because it ran
out of room.

`Scheduled 0 | Skipped 3 | Total 2/243 | Buffer until 2026-09-25 | Ramadan series 0/12 done | Hajj series 1/8 done | Next run resumes at slot #7`

Nothing was pushed: no draft passed both reviewers, so there is nothing for the ingest to schedule. The system fixes are committed and verified by an isolated build — 306 static pages, SEO suite 282 pages (249 indexable) with 0 failures, schema 78/78, unit tests 90/90 — and ship with the next deploy.

## Slot outcomes

| Slot | Topic | Result | Scores (FR / AR) | Why |
|---|---|---|---|---|
| #5 | premiere-omra-p1 — ready to go? | **Skipped** after the one rewrite | 6.6 → 7.0 / 6.8 → 7.4 | Readiness is mostly experiential. The rewrite fixed the Hajj-channel error and the unattributed observations; reviewers still found an unsourced line on payment terms, an unsourced religious judgement on intention, FAQs repeating the body, and (AR) an imprecise miqat definition |
| #6 | ville-rabat — reaching Mohammed V from Rabat | **Skipped** after the one rewrite | 7.4 → 7.6 / 8.0 → 8.4 | The rewrite fixed v1's unsourced geography, the generalising excerpt and two duplicated FAQs, and the Arabic passed. The French still wrote the passport rule's « six mois » in prose (the key's note forbids the figure), kept an off-angle FAQ, echoed `/fr/omra-depuis-rabat`, and repeated « aucun paiement en ligne » five times. Underneath: for a Rabat reader almost every sourced fact is already on the commercial page, and the guide format's floor pushes the post to say it again |
| #7 | seasonal-coupures-scolaires-structure | **Not written** — slot left `planned` | — | The angle maps five breaks « of different lengths ». The only source, `official.ecole_ma_calendrier_2026_2027` (search-summary, structure only), gives five breaks of the same short span. Writing to the angle invents the lengths; writing to the data duplicates `/blog/omra-vacances-scolaires`. The topic needs replanning, not a post |
| #8 | hajj-1448-p2 — conditions | **Skipped**, rewrite not attempted | 6.2 / 7.2 | Every conditions fact is already in part 1; what the part was to add has no key (see below) |
| #9 | premiere-omra-p2 — the contract | Not drafted | — | The contract clauses the outline lists (board, transport, cancellation and modification terms) have no key; only « contrat écrit, reçu, inclusions sur la fiche, aucun paiement en ligne » exist |
| #10 | Rajab / Sha'ban vs Ramadan | Not drafted | — | Crowd density, ease of the rites and effort are experiential; no `month_pages` for those months |
| #11 | hajj-documents-a-preparer | Not drafted | — | Identity card, civil-status spelling and transliteration as a rejection cause: no key |
| #12 | ville-marrakech | Writable — next run | — | `logistique_marrakech` and its two FAQs are consistent. Write it with slot #6's final review in hand, and keep it clear of the Rabat post (G15, and the reviewers' usefulness test) |
| #13 | premiere-omra-p3 — the file | Writable but thin — next run | — | Covered by `passport_validity`, `visa_included`, `children` and the Nusuk structure, but `/guide-omra/documents-visa` and `/blog/application-nusuk-guide` already say most of it; only the order of operations is new |

## What each blocked slot needs

- **#8, #11 (Hajj):** the season's Habous announcement, verified in a browser and added to
  `data/official-facts.json` with `verification: "fetched"` — who assesses physical fitness,
  companion arrangements, any priority rule, disability handling, whether the ten-year rule
  covers agency registrations, and the registration document list.
- **#9 (contract):** the agency's standard contract clauses and its cancellation and modification
  terms, added to `policies`.
- **#10 (Rajab / Sha'ban):** the authored `month_pages` blocks for those months, and the owner's
  decision on citable agency observation (RESUME, owner item 0).
- **#7:** a replanned angle in `data/content-topics/seasonal.json` that the school-calendar data
  supports and that `/blog/omra-vacances-scolaires` does not already cover — or drop the topic.

## Defects found and fixed at system level

Each was fixed at the rule, re-run over every draft and the scheduled queue, and none moved the
bar: the queue stayed at 4 pass / 8 fail through every change.

| Commit | Defect | Fix |
|---|---|---|
| `abb3b3f` | `--slug` failed G13/G16 on a scheduled row that `--existing=scheduled` passed | every loaded row carries its state; the backlog exit rule stays specific to `--existing` |
| `3c351c0` | G6 re-derived a draft's cluster from owner page + category and ignored the slot's planned cluster and siblings: 66 of 165 planned slots could not reach two siblings, and the 13 owned by `/bab-makka` never could | G6 counts the planned cluster and the slot's non-lander `sibling_paths`; 165/165 now reachable |
| `e5dfcd4` | The Arabic month rule matched substrings: « آب » (August) inside « الآباء » (the parents) | whole-word match with Arabic proclitics, built with `String.raw`; `tests/arabic-months.test.mjs` added to `npm test` (90/90) |
| `e5dfcd4` | **The ingest ignored `status: skipped`.** A skipped draft that passed the mechanical gate was inserted as a scheduled row — which is how the two skipped posts of 2026-09-14 became rows at all | the ingest refuses a skipped draft and logs why |
| `e5dfcd4` | G6 failed a link to a sibling post that is scheduled but goes live first — every series part linking its previous part | such a link passes when the target publishes no later than the draft's own slot |

## Specification conflicts for the owner

- **The WhatsApp closing paragraph.** The brief puts it last in the body, but `extractFaq` reads
  any paragraph after the FAQ as part of the last answer. Drafts keep it just before the FAQ.
- **The brand in Arabic.** The AR reviewer brief writes « Wiki Tours International (باب مكة) »;
  every Arabic post, scheduled ones included, uses « ويكي تورز إنترناشيونال (باب مكة) ». Pick one.
- **City slots.** The Rabat and Marrakech outlines ask for `<LiveDepartures>` while their `delta`
  says the article « lists no offer ».
- **Rabat journey time.** `faqs/b3428e5a` says « un peu plus d'une heure », `faqs/dbf60b00`
  « environ une heure trente ». No post states either until the admin FAQs agree.

## Needs the owner now

1. **Two skipped posts are set to go public.** `guide-omra-ramadan-1-vivre-le-mois` (21 Sep) and
   `omra-en-novembre-meteo-affluence-conseils` (24 Sep) were switched back to `is_published = true`
   on 2026-09-15 at 11:38 UTC, four seconds apart. No build, deploy or script in this session did
   it: the local builds had no database credentials, nothing was pushed, `toArticleRow` never sets
   `is_published`, and no other session was online. It reads as a manual change in the admin or the
   Supabase dashboard. If it was deliberate, nothing to do; if not, unpublish both before 21 Sep.
2. Apply migrations 025 and 026, and export Search Console into `data/gsc/` (audit, owner items 1–2).
