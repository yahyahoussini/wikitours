# i18n audit — untranslated strings

Generated by `npm run i18n:audit -- --write` (`scripts/i18n-audit.mjs`). Re-run it
after touching `src/i18n/*.json` or adding user-visible copy.

This is a **report, not a gate** — it always exits 0. Several entries below are
legitimate (proper nouns, words that are genuinely identical in French and
English). A wrong Arabic translation is worse than an obviously untranslated
string, so anything ambiguous is listed for human review rather than guessed.

Allowlisted as legitimately non-translated: `Wiki Tours`, `WIKI TOURS`, `Wiki Tours International`, `Bab Makka`, `Bab Makkah`, `Saudia`, `Anjum`, `Abraj Al Kiswah`, `Makarem Madinah`, `Swiss International`, `Taj Park`, `Emaar Grand`, `Dhiafat Al Rajaa`, `Jayden Medina`, `Al Boraq`, `WhatsApp`, `Google`, `Facebook`, `Instagram`, `TikTok`, `YouTube`, `Nusuk`, `ODV-25012`, `MAD`, `VIP`, `ISO`, `SEO`, `URL`, `CIN`, `Labbayk Allahumma labbayk`, `[CONTENT NEEDED]`, `[TRANSLATION NEEDED]`.

## Summary

| Check | Count |
|---|---|
| Latin script inside an `ar` dictionary value | 0 |
| `en` value byte-identical to `fr` | 26 |
| Key missing from `ar` or `en` (silent French fallback) | 0 |
| Hardcoded user-visible literal in source | 0 |

## 1. Latin script inside an `ar` value

_None._

## 2. `en` identical to `fr` — review, often legitimate

All reviewed; no action outstanding beyond the rows marked REVIEW.

| Key | Value | Verdict |
|---|---|---|
| `nav.hajj` | Hajj | same word |
| `nav.destinations` | Destinations | same word |
| `nav.blog` | Blog | same word |
| `nav.contact` | Contact | same word |
| `brand.byParent` | by WIKI TOURS | brand lockup |
| `home.selectorOccasion` | Occasion | same word |
| `a11y.sections` | Sections | same word |
| `articleCategory.omra` | Omra | proper noun |
| `articleCategory.hajj` | Hajj | proper noun |
| `articleCategory.guide` | Guide | same word |
| `offer.datesLabel` | Dates | same word |
| `offer.conditionsTitle` | Conditions | same word |
| `offer.tier.premium` | Premium | same word |
| `offer.roomShort.double` | double | same word |
| `offer.roomShort.triple` | triple | same word |
| `offer.roomShort.quad` | quadruple | same word |
| `offer.roomShort.quint` | quintuple | same word |
| `offer.subnav.programme` | Programme | REVIEW — British "Programme" vs American "Program" |
| `offer.subnav.conditions` | Conditions | same word |
| `cityPage.seoTitleQualifierShort` | Mohammed V | proper noun (airport) |
| `cities.casablanca` | Casablanca | same exonym |
| `cities.rabat` | Rabat | same exonym |
| `cities.agadir` | Agadir | same exonym |
| `cities.oujda` | Oujda | same exonym |
| `pages.contactTitle` | Contact | same word |
| `pages.ramadanShort` | Omra Ramadan | deliberate — set to the English form on purpose |

## 3. Missing keys (silent French fallback)

_None — fr / ar / en are at full key parity._

## 4. Hardcoded user-visible literals in source

These never reach a dictionary, so no locale can override them. `jsx-text` is
rendered copy; the rest are attributes read by screen readers.

_None._
